version: '3.8'

services:
  luminark-api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - .:/app
    restart: always
  # Optional: Future Database (Postgres)
  # db:
  #   image: postgres:13
  #   environment:
  #     POSTGRES_DB: luminark
  #     POSTGRES_PASSWORD: secretpassword
